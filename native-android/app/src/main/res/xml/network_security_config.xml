<?xml version="1.0" encoding="utf-8"?>
<!--
  Release policy: HTTPS only, no exceptions. The debug variant overrides this
  file to permit cleartext to localhost for `adb reverse` development.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
