<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Pre-31: the window background is what shows during launch. -->
        <item name="android:background">@drawable/splash</item>
        <!-- API 31+: the platform draws its own splash and ignores the
             background above, which left a light grey flash before this dark
             app appeared. core-splashscreen's v31 variant forwards this to
             android:windowSplashScreenBackground, so the platform splash
             centres the launcher icon on the app's own ground. No call to
             installSplashScreen is needed for this, and postSplashScreenTheme
             is deliberately not set — BridgeActivity never installs the
             splash, so nothing would switch the theme back. -->
        <item name="windowSplashScreenBackground">@color/auraBackground</item>
    </style>
</resources>